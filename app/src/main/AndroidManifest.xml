<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project_uts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ObatActivity"
            android:label="@string/title_activity_obat"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".FasilitasActivity"
            android:label="@string/title_activity_fasilitas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TambahObat" />
        <activity android:name=".TambahFasilitas" />
        <activity
            android:name=".DokterActivity"
            android:label="@string/title_activity_dokter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DetalPasienActivity" />
        <activity
            android:name=".PasienActivity"
            android:label="@string/title_activity_pasien"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TambahPasien" />
        <activity android:name=".TentangActivity" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>